require 'spec_helper'
require "#{Rails.root}/lib/mail_bot"

describe "MailBot::Bot" do
  
  describe "bootstrapping Mailbot" do
    it "should require a configuration instance" do
      # pending
    end
    
    it "should allow the mailing list to be specified on #run" do
      # pending
    end
  end

  
  
end