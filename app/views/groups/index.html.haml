- @groups.each do |group|
  .entries
    %p
      = link_to group.name, group_conversations_path(group) 
    %p
      = "#{pluralize(group.conversations_count, "conversation")} | #{pluralize(group.members_count, "member")}"
